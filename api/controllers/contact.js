module.exports = {

  // GET /contact/{id}
  get(req, res) {
    res.json({task: 'TODO'});
  },

  // PUT /contact/{id}
  put(req, res) {
    res.json({task: 'TODO'});
  },

  // DELETE /contact/{id}
  delete(req, res) {
    res.json({task: 'TODO'});
  }
}